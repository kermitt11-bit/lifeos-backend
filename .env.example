# Set these in Vercel / your hosting environment.
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Optional shared secret. If set, the API requires `Authorization: Bearer <token>`.
API_TOKEN=

PORT=3000
